
  <article>
    <header>
      <h2><%= @movie.title %></h2>
    </header>
      <p><%= @movie.description %></p>
    <table>
      <tr>
        <th>Released:</th>
        <td><%= @movie.released_on.to_s(:release_date) %> (<%= time_ago_in_words(@movie.released_on) %> ago) </td>
      </tr>
      <tr>
        <th>Rating:</th>
        <td><%= @movie.rating %></td>
      </tr>
      <tr>
        <th>Total Gross:</th>
        <td><%= format_total_gross(@movie) %> </td>
      </tr>
    </table>
  </article>

<%= link_to "All Movies", movies_path %> <!-- OR link_to("All Movies", movies_path) --> | 
<%= link_to "Edit This Movie", edit_movie_path %>