<header id="content-header">
  <h1><%= pluralize(@users.size, 'User') %></h1>
</header>

<ul id="users", class="index-image">
  <% @users.each do |user| %>
    <li><%= profile_image_for(user) %> <%= link_to user.name, user %> (<%= user.email %>)</li>
  <% end %>
</ul>
    